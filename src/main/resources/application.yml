server:
  port: 8080
  servlet:
    context-path: /demo

#spring:
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/demo01?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
#    username: root
#    password: 123456

# 日志配置
logging:
  config: classpath:log4j2.xml

# MyBatis配置（如果您计划使用MyBatis）
# mybatis:
#   mapper-locations: classpath:mapper/*.xml
#   type-aliases-package: com.study.demo01.entity
#   configuration:
#     map-underscore-to-camel-case: true

# 自定义配置
app:
  name: demo01
  version: 1.0.0
  description: Spring Boot Demo Application